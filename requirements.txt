# What the deployed Streamlit app needs. Kept lean on purpose so the cloud build is
# fast and reliable. The notebooks use a few extras (lightgbm, shap, optuna, matplotlib)
# that are not needed to run the app.
streamlit
plotly
pandas
numpy<2
scikit-learn
imbalanced-learn
reportlab
requests
lxml
