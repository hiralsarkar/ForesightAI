# Normalise line endings: LF in the repo, native in the working tree.
* text=auto eol=lf

# Binary assets - never touch line endings or diff as text.
*.pdf binary
*.pptx binary
*.joblib binary
*.png binary
*.arff binary

# Notebooks: keep JSON intact.
*.ipynb text eol=lf
