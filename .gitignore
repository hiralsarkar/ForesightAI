# Secrets -- NEVER commit. Contains the OpenRouter API key.
secrets.local.json
.env
.streamlit/secrets.toml

# Environment / local tooling
.venv/
.claude/
__pycache__/
*.pyc
.pytest_cache/

# Local-only test suite (kept off the public repo)
tests/
pytest.ini

# Data & artefacts (regenerable)
data/raw/*.arff
data/raw/*.zip
models/*.joblib
reports/*.pdf

# OS
.DS_Store
Thumbs.db

# Generated report images (regenerable)
reports/*.png
reports/*.svg

# Internal working notes - not part of the public repo
AGENTS.md
UNDERSTANDING.md

# deck build deps
deck/node_modules/

# Office lock/temp files
~$*
